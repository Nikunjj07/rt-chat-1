A basic chat app where you can come in, join the rooms and exchange messages with other people

To run locally:

Start server: 
`
cd chat-server
npm install
npm run dev
`
Start Client:
`
cd chat-client
npm install
npm run dev
`